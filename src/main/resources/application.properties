spring.application.name=ecommerce-backend

# ==========================================
# Configuración de Base de Datos (Supabase - PostgreSQL)
# ==========================================
# Reemplaza con los datos de tu proyecto de Supabase:
# Settings -> Database -> Connection string (URI / JDBC / Session Pooler)
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-0-us-west-2.pooler.supabase.com:6543/postgres?sslmode=require}
spring.datasource.username=${DB_USERNAME:postgres.wuasopsogkicecxozyvj}
spring.datasource.password=${DB_PASSWORD:MkDmpwq6G5DBGk78}
spring.datasource.driver-class-name=org.postgresql.Driver

# ==========================================
# Configuración de Hibernate / JPA
# ==========================================
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false
